# Leave empty to use the Vite dev proxy to the API (see vite.config.ts).
# For production builds served separately from the API, set the full origin, e.g.:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
